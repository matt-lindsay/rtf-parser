{
  "name": "rtf2text-test",
  "version": "1.0.0",
  "description": "This project is a working code snippet of the rtf-parser npm module, which will be built into other projects.",
  "main": "app.js",
  "scripts": {
    "prestart": "npm install",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "matt lindsay",
  "license": "MIT",
  "dependencies": {
    "rtf-parser": "^1.1.0"
  }
}
