'use strict';

var Rem1Service = function() {

        // ...add a newline prior to the start of a new record. The first record will require its
        // preceeding newline character to be removed.
        //if (counter === 0 ) {
 };
module.exports = Rem1Service;
